# ================================================================
# Delve Backend Configuration
# ================================================================

# Delve API URL - The backend service that handles OnchainFi payments and knowledge graph operations
# Local development: http://localhost:8000
# Production: https://api.delve.example.com
NEXT_PUBLIC_DELVE_API_URL=http://localhost:8000

# ================================================================
# Payment Configuration (Client-Side)
# ================================================================

# Payment amount in token units (e.g., "0.01" for 0.01 USDC)
# For tokens with decimals, you can use decimal notation or full units
# Examples: "0.01" (0.01 USDC), "1000000" (1 USDC with 6 decimals)
NEXT_PUBLIC_PAYMENT_AMOUNT=0.01

# Chain ID for wallet connection
# 8453 = Base Mainnet
# 11124 = Abstract Testnet
# 1 = Ethereum Mainnet
NEXT_PUBLIC_CHAIN_ID=11124

# ERC-20 token contract address for payments (e.g., USDC address)
# Base Mainnet USDC: 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
# Abstract Testnet Token: (replace with actual test token address)
NEXT_PUBLIC_PAYMENT_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# Wallet address that will receive payments
# IMPORTANT: This MUST match the recipient address configured in delve's OnchainFi settings
NEXT_PUBLIC_PAYMENT_RECIPIENT_ADDRESS=0x0000000000000000000000000000000000000000

# Blockchain network name (must match delve's configuration)
# Options: "base", "ethereum", "abstract"
NEXT_PUBLIC_PAYMENT_NETWORK=abstract

# ================================================================
# Microsub Configuration (Optional)
# ================================================================

# Default number of queries allowed per microsub (payment subscription)
# When a user pays once, they can make this many queries before needing to pay again
# Default: 25
NEXT_PUBLIC_QUERY_LIMIT=25

# Default microsub expiration in days
# How long the microsub remains valid after payment
# Default: 30
NEXT_PUBLIC_EXPIRATION_DAYS=30

# ================================================================
# Application Configuration (Optional)
# ================================================================

# Default agent ID for demos (optional)
# If not set, users must provide an agent ID via URL parameter
NEXT_PUBLIC_DEFAULT_AGENT_ID=

# Environment (development, production, test)
NODE_ENV=development

# ================================================================
# Scaffold-ETH 2 Configuration (Keep Existing)
# ================================================================

# Alchemy API key for RPC endpoints (optional but recommended)
NEXT_PUBLIC_ALCHEMY_API_KEY=

# WalletConnect project ID for RainbowKit
# Get one at https://cloud.walletconnect.com
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# ================================================================
# Hardhat Configuration (packages/hardhat/.env)
# ================================================================

# Private key for contract deployment (keep in packages/hardhat/.env)
# DEPLOYER_PRIVATE_KEY=0x...

# ================================================================
# Setup Instructions
# ================================================================

# 1. Copy this file to .env.local:
#    cp .env.example .env.local

# 2. Update the payment configuration values:
#    - Set NEXT_PUBLIC_PAYMENT_TOKEN_ADDRESS to your ERC-20 token address
#    - Set NEXT_PUBLIC_PAYMENT_RECIPIENT_ADDRESS to your receiving wallet
#    - Ensure these match your delve backend OnchainFi configuration

# 3. Configure delve backend URL:
#    - For local development: http://localhost:8000
#    - For production: your deployed delve API URL

# 4. Obtain API keys:
#    - WalletConnect Project ID: https://cloud.walletconnect.com
#    - Alchemy API Key (optional): https://www.alchemy.com

# 5. Start the development server:
#    yarn dev

# ================================================================
# Important Notes
# ================================================================

# - No OnchainFi API keys are needed in Next.js - delve backend handles all payment verification
# - Payment recipient address MUST match delve's OnchainFi configuration
# - Token address MUST match delve's OnchainFi configuration
# - Network name MUST match delve's OnchainFi configuration
# - For testnet development, use test tokens and testnet addresses
